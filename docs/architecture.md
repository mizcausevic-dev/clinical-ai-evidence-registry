# Architecture

The registry separates evidence work into four lanes:

- Python builds executive and investor diligence packs.
- R summarizes cohort coverage and release blockers.
- SQL defines warehouse views for model evidence and board posture.
- TypeScript renders the release-readiness surface.

The fixture is synthetic and intentionally contains no PHI.
